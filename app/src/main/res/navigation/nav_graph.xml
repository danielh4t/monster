<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/BeastsFragment">

    <fragment
        android:id="@+id/BeastsFragment"
        android:name="app.stacq.monster.ui.beasts.BeastsFragment"
        android:label="@string/beasts"
        tools:layout="@layout/fragment_beasts">

        <action
            android:id="@+id/action_Beasts_to_Unleashed"
            app:destination="@id/BeastFragment" />
    </fragment>
    <fragment
        android:id="@+id/BeastFragment"
        android:name="app.stacq.monster.ui.beast.BeastFragment"
        android:label="@string/beast"
        tools:layout="@layout/fragment_beast">
        <action
            android:id="@+id/action_Beast_to_Beasts"
            app:destination="@id/BeastsFragment" />
    </fragment>
</navigation>